setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY}
    DEBUG: 0

build:
  docker:
    web: Dockerfile

release:
  image: bubhux/bubhux-oc-image-build:latest
